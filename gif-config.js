// Chave gratuita da API do GIPHY, usada só pra buscar GIFs — não tem nada a
// ver com o Firebase. Como conseguir a sua:
//
// 1. Acesse https://developers.giphy.com e crie uma conta (grátis).
// 2. No painel ("Dashboard"), clique em "Create an App".
// 3. Escolha "API" (não "SDK"), dê um nome qualquer (ex: "bapo") e confirme.
// 4. Sua chave aparece na tela do app, em "API Key". Copie ela.
// 5. Cole o valor abaixo, no lugar de "SUBSTITUA_AQUI".
//
// O plano grátis já vem liberado (chave "beta", com limite de buscas por
// hora bem generoso pra uso pessoal). Enquanto não configurar, a aba de GIFs
// mostra um aviso em vez de tentar buscar.
export const GIPHY_API_KEY = "SUBSTITUA_AQUI";
